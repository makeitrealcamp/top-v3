/*    Pseudocodigo
 *
 *    - Entrada un string con un nombre
 *    - Salida: Devolver la palabra "Hello" seguida del nombre que le dimos por entrada
 *    - Instrucciones:
 *        * Devolver la concatenacion de la palabra "Hello" y el string de entrada separados por un
 *        espacio
 *
 * */

function hello_world(name) {
  return "Hello " + name
}

hello_world("Pedro")
hello_world("Maria")
